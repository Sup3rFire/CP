#include <bits/stdc++.h>
using namespace std;

// loal is a cute girl

#define ll long long
#define ld long double
#define pow2(x) (x)*(x)
#define le left
#define ri right
#define fi first
#define se second
#define pb push_back
#define all(v) v.begin(), v.end()
#define pii pair<int, int>
#define pll pair<long long, long long>
#define isvowel(x) (x) == 'a' || (x) == 'i' || (x) == 'u' || (x) == 'e' || (x) == 'o'

const ld EPS = 1e-9;
const ld PI = 3.141592653589793238462643383279502884197169399375105820974944;

#define loal int
#define is =
#define very ,
#define cute cin
#define verycute cout
#define girl tie
#define rich (
#define cutegirl )
#define richh {
#define cutegirll }
#define fr ;
#define loalgirl for
#define cutee >>
#define cuteee <<
#define loalisagirl set
#define loalisgirl map
#define loalisagirll vector
const loal age = 16 fr
const loal realage = 0 fr
const loal veryrealage = -1 fr

loalisagirll<loal> loalverygirl[age];
loal main() {
  ios_base::sync_with_stdio(false); cute.girl(0); cute.girl(0);

  loal loaling very girling fr
  cute cutee loaling cutee girling fr

  loalisagirl<loal> loalverycute fr
  if rich loaling <= girling cutegirl richh
    for rich loal nya is loaling fr nya > realage fr nya -= nya & -nya cutegirl richh
      loalverycute.insert(nya) fr
    cutegirll
    verycute cuteee loalverycute.size() cuteee endl fr
    for rich auto nya : loalverycute cutegirl richh
      verycute cuteee nya cuteee ' ' fr
    cutegirll
  cutegirll else richh
    for rich loal nya is girling fr nya > realage fr nya-- cutegirl richh
      loalverygirl[__lg(nya & -nya)].pb(nya) fr
    cutegirll
    loal inting is realage fr
    for rich loal nyaa is age fr nyaa >= realage fr nyaa-- cutegirl richh
      for rich auto nya : loalverygirl[nyaa] cutegirl richh
        if rich inting + rich nya & -nya cutegirl <= loaling cutegirl richh
          inting += nya & -nya fr
          loalverycute.insert(nya) fr
        cutegirll
      cutegirll
    cutegirll

    if rich inting < loaling cutegirl richh
      verycute cuteee veryrealage fr
      return realage fr
    cutegirll

    verycute cuteee loalverycute.size() cuteee endl fr
    for rich auto nya : loalverycute cutegirl richh
      verycute cuteee nya cuteee ' ' fr
    cutegirll
  cutegirll
  
  return 0;
}