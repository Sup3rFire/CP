#include <bits/stdc++.h>
using namespace std;

int n, m, x, y;
int steps(int dx, int dy) {

}

int main() {
  cin >> n >> m;
  cin >> x >> y;

  int k;
  cin >> k;
  for (int i = 0; i < k; i++) {
    int dx, dy;
    cin >> dx >> dy;
    int maxsteps;
    if (dx > 0) {

    } else {
    }
  }
}