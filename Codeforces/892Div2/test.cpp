#include <bits/stdc++.h>
using namespace std;

int main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(0);

  for (int i = 1; i <= 20; i++)
  {
    cout << (i + 3) / 4 + 1 << " " << i << endl;
  }

  return 0;
}